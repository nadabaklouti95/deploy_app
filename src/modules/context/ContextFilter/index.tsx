import * as React from "react";
import { IContextFilter } from "types/interfaces/ContextInterface";
 
const ContextFilter: React.FC<IContextFilter> = (props) => {

  return (
   <div>ContextFilter</div>
  );
  
};

export default ContextFilter;
